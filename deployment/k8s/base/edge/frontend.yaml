apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: af-edge
spec:
  selector:
    app: substrate-ui
  ports:
    - port: 3000
      targetPort: 3000
---
# NOTE: Add a Deployment for substrate-ui here or use an external frontend deployment.
# This Service stub ensures the Ingress backend exists.
# Example:
#   apiVersion: apps/v1
#   kind: Deployment
#   metadata:
#     name: substrate-ui
#     namespace: af-edge
#   spec:
#     selector:
#       matchLabels:
#         app: substrate-ui
#     template:
#       metadata:
#         labels:
#           app: substrate-ui
#       spec:
#         containers:
#           - name: substrate-ui
#             image: ghcr.io/your-org/substrate-ui:latest
#             ports:
#               - containerPort: 3000
