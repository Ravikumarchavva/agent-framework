apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: af-edge
spec:
  selector:
    app: ravi-ui
  ports:
    - port: 3000
      targetPort: 3000
---
# NOTE: Add a Deployment for ravi-ui here or use an external frontend deployment.
# This Service stub ensures the Ingress backend exists.
# Example:
#   apiVersion: apps/v1
#   kind: Deployment
#   metadata:
#     name: ravi-ui
#     namespace: af-edge
#   spec:
#     selector:
#       matchLabels:
#         app: ravi-ui
#     template:
#       metadata:
#         labels:
#           app: ravi-ui
#       spec:
#         containers:
#           - name: ravi-ui
#             image: ghcr.io/your-org/ravi-ui:latest
#             ports:
#               - containerPort: 3000
