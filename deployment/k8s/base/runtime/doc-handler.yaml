# Document-Handling Service — Kubernetes Deployment
#
# Layout-aware document parsing (PDF layout, chart/table detection, OCR) plus
# multimodal embedding (SigLIP) and reranking (MiniLM cross-encoder) for chat
# attachments and the local RAG backend, isolated from the main API process
# — it pulls paddlepaddle + torch (via sentence-transformers) and model
# inference is slow enough that running it inline would block chat
# responses (see pyproject.toml's `doc-handler` extra comment). CPU-only —
# no GPU/nvidia device request needed (paddlepaddle's CPU wheel is used).
#
# Unlike code-interpreter, this is a plain Deployment, not a StatefulSet:
# doc-handler is stateless request/response with no session affinity, so
# there's no need for consistent-hash routing or per-pod identity. No HPA
# either — model-loaded pods are expensive to spin up, so CPU-based
# autoscaling churn costs more than it saves at this scale; scale by
# editing `replicas` directly if throughput becomes a bottleneck.
#
# Optional: the monolith/services only call this when DOC_HANDLER_SERVICE_URL
# is set (see config.py). Not setting it (or this Deployment not existing)
# just means chat attachments fall back to the lightweight pypdf path for
# PDFs — no hard dependency.

apiVersion: v1
kind: Service
metadata:
  name: doc-handler
  namespace: af-runtime
  labels:
    app: doc-handler
spec:
  selector:
    app: doc-handler
  ports:
    - port: 8080
      targetPort: http
      name: http
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: doc-handler
  namespace: af-runtime
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: doc-handler
  namespace: af-runtime
  labels:
    app: doc-handler
    af.service-role: doc-handler
spec:
  replicas: 1
  selector:
    matchLabels:
      app: doc-handler
  template:
    metadata:
      labels:
        app: doc-handler
        af.service-role: doc-handler
    spec:
      serviceAccountName: doc-handler
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
      containers:
        - name: doc-handler
          image: ghcr.io/example/agent-framework/doc-handler:0.1.0
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false  # caches model weights under $HOME
            capabilities:
              drop: ["ALL"]
          ports:
            - containerPort: 8080
              name: http
          envFrom:
            - secretRef:
                name: runtime-secrets
          env:
            - name: SERVICE_NAME
              value: doc-handler
          resources:
            requests:
              cpu: 1000m
              memory: 2Gi
            limits:
              cpu: 4000m
              memory: 6Gi
          livenessProbe:
            httpGet:
              path: /v1/health
              port: http
            initialDelaySeconds: 90
            periodSeconds: 20
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /v1/health
              port: http
            initialDelaySeconds: 60
            periodSeconds: 15
            failureThreshold: 3
