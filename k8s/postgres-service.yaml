apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: agent-framework
spec:
  selector:
    app: postgres
  ports:
  - port: 5434
    targetPort: 5434
  clusterIP: None  # Headless service for StatefulSet
