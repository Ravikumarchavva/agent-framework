[project]
name = "ravi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ravikumar Chavva", email = "chavvaravikumarreddy2004@gmail.com" },
]
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27.0",
    "mcp>=1.0.0",
    "openai>=2.15.0",
    "opentelemetry-api>=1.20.0",
    "opentelemetry-sdk>=1.20.0",
    "pydantic>=2.12.5",
    "pydantic-settings>=2.12.0",
    "python-json-logger>=2.0.7",
    "tiktoken>=0.12.0",
    "fastapi>=0.128.0",
    "redis[hiredis]>=5.0.0",
    "sqlalchemy>=2.0.46",
    "asyncpg>=0.30.0",
    "uvicorn[standard]>=0.34.0",
    "pyyaml>=6.0",
    "python-multipart>=0.0.9",
    "rich>=14.3.2",
    "pyjwt>=2.12.0",
    "cryptography>=46.0.6",
    "apscheduler>=4.0.0a5",
    "anthropic>=0.95.0",
    "google-genai>=1.73.0",
    "k8s-agent-sandbox>=0.4.6",
    "msgspec>=0.21.1",
    "pillow>=10.4.0",
    "markitdown-ocr>=0.1.0",
    "ipywidgets>=8.1.8",
    "opentelemetry-exporter-otlp<1.20",
    "opentelemetry-instrumentation-fastapi<0.60b1",
    "paddleocr[all]",
    "ipykernel>=7.2.0",
    "playwright>=1.60.0",
    "psycopg[binary]>=3.3.4",
    "pgvector>=0.4.2",
    "ddgs>=9.14.4",
    "crawl4ai>=0.8.7",
    "pypdf>=6.13.0",
    "docling>=2.0.0",
    "aiobotocore>=3.7.0",
    "botocore>=1.43.0",
]

[project.scripts]
ravi = "ravi.cli:main"
start = "ravi.cli:start_main"

[build-system]
requires = ["uv_build>=0.11.16,<0.12"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "import-linter>=2.11",
    "ipykernel>=7.2.0",
    "pytest>=9.0.3",
    "pytest-asyncio>=1.4.0",
    "ruff>=0.15.15",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["."]


[tool.ruff]
exclude = ["notebooks/", "examples/", ".venv"]

[tool.ruff.lint]
# E701/E702: single-line if/semicolons are intentional in dispatch tables
# E402: mid-file imports are intentional in some route files
[tool.ruff.lint.per-file-ignores]
"src/ravi/tools/code_interpreter/guest_agent/agent.py" = ["E701", "E702"]
"src/ravi/services/conversation/routes.py" = ["E402"]
# plugin/registry.py deliberately imports base classes at the bottom of the
# module to avoid circular imports with kernel.{agents,guardrails,...}.
"src/ravi/kernel/plugin/registry.py" = ["E402"]

# ---------------------------------------------------------------------------
# import-linter — enforce the layered architecture so the kernel stays frozen.
# Layers (strictly downward dependencies only):
#   server | services  ← catalog  ← integrations  ← extensions  ← kernel
# Run with: uv run lint-imports
# ---------------------------------------------------------------------------
[tool.importlinter]
root_packages = ["ravi"]

[[tool.importlinter.contracts]]
name = "four stack layers"
type = "layers"
layers = [
    "ravi.fabric",        # L3 — orchestration programs (flows, evals, durable)
    "ravi.capabilities",  # L2 — tools, skills, knowledge, connectors
    "ravi.agents",        # L1 — core intelligence, LLM, guardrails, middleware
    "ravi.kernel",        # L0 — pure contracts: Protocols, dataclasses, enums
]

[[tool.importlinter.contracts]]
name = "agents cannot import capabilities or fabric"
type = "forbidden"
source_modules = ["ravi.agents"]
forbidden_modules = ["ravi.capabilities", "ravi.fabric"]

[[tool.importlinter.contracts]]
name = "capabilities cannot import fabric"
type = "forbidden"
source_modules = ["ravi.capabilities"]
forbidden_modules = ["ravi.fabric"]

[[tool.importlinter.contracts]]
name = "kernel is independent"
type = "forbidden"
source_modules = ["ravi.kernel"]
forbidden_modules = [
    "ravi.agents",
    "ravi.capabilities",
    "ravi.fabric",
    "ravi.integrations",
    "ravi.serving",
    "ravi.config",
    "ravi.logger",
]

[[tool.importlinter.contracts]]
name = "serving cannot import agents or capabilities"
type = "forbidden"
source_modules = ["ravi.serving"]
forbidden_modules = ["ravi.agents", "ravi.capabilities", "ravi.fabric"]
# infrastructure/ is orthogonal — it may cross-cut to agents/capabilities by design.
# We only want to catch DIRECT imports from serving/ to agents/capabilities.
allow_indirect_imports = "true"
ignore_imports = [
    # current_thread_id ContextVar spans both chat.py and capabilities/task_manager
    "ravi.serving.monolith.routes.chat -> ravi.capabilities.tools.task_manager.tool",
    # WebHITLBridge delivers human-input events; must know the capability type
    "ravi.serving.monolith.sse.bridge -> ravi.capabilities.tools.human_input",
    # pipelines/triggers routes need DTOs defined in capabilities (PipelineDef, TriggerDef, ConditionDef)
    "ravi.serving.monolith.routes.pipelines -> ravi.capabilities.pipeline.engine",
    "ravi.serving.monolith.routes.triggers -> ravi.capabilities.triggers.conditions",
    "ravi.serving.monolith.routes.triggers -> ravi.capabilities.triggers.scheduler",
    # agent_runtime: in-memory fallback Runtime (for RUNTIME_BACKEND=memory env)
    "ravi.serving.services.agent_runtime.app -> ravi.agents.runtime",
    # agent_runtime service: load_session_memory is an L1 concern with no kernel protocol yet
    "ravi.serving.services.agent_runtime.service -> ravi.agents.factory",
    # tool_executor and code_interpreter ARE capability executors by design
    "ravi.serving.services.tool_executor.app -> ravi.agents.storage.tasks",
    "ravi.serving.services.tool_executor.app -> ravi.capabilities.tools.code_interpreter.http_client",
    "ravi.serving.services.tool_executor.app -> ravi.capabilities.tools.code_interpreter.tool",
    "ravi.serving.services.tool_executor.app -> ravi.capabilities.tools.task_manager.tool",
    "ravi.serving.services.tool_executor.app -> ravi.capabilities.tools.web.surfer",
    "ravi.serving.services.code_interpreter.app -> ravi.capabilities.tools.code_interpreter.config",
    "ravi.serving.services.code_interpreter.app -> ravi.capabilities.tools.code_interpreter.session_manager",
    "ravi.serving.services.code_interpreter.schemas -> ravi.capabilities.tools.code_interpreter.schemas",
]

[tool.uv.sources]
paddleocr = { git = "https://github.com/PaddlePaddle/PaddleOCR.git" }

# Orthogonal peers (not part of the layer stack):
# integrations/ — concrete I/O ports (LLM clients, memory backends, vector stores)
# serving/      — FastAPI monolith + microservices + shared infra
# These carry cross-layer edges by design (e.g. an integration implements a kernel
# Protocol; serving wires all layers together in lifespan).
